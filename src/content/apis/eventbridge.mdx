---
title: 'eventbridge.sh — webhook delivery API'
type: api
oneLiner: 'Webhook delivery and replay API for event-driven services'
stack: ['Go', 'SQLite', 'SSE']
featured: true
order: 3
docsUrl: 'https://docs.example.com/eventbridge' # TODO: replace with real Swagger/OpenAPI docs URL
endpoints:
  - method: POST
    path: /events
    summary: 'Publish an event for delivery'
    auth: 'Bearer token (X-API-Key header)'
  - method: GET
    path: /events/{id}
    summary: 'Fetch delivery history and status for one event'
    auth: 'Bearer token (X-API-Key header)'
  - method: POST
    path: /events/{id}/replay
    summary: 'Re-deliver a failed webhook immediately'
    auth: 'Bearer token (X-API-Key header)'
---

Built to move events reliably between services. Delivery is at-least-once, every attempt persisted, and consumers can replay any event on demand. The full OpenAPI spec lives in Swagger; the endpoints below are the ones I reach for daily.
